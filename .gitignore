# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Platform runtime output — regenerated by `--stage pipeline`
data/
pipeline_execution.log

# Optional: after a live run against ClinicalTrials.gov you may force-add the
# cached raw response so reviewers can replay the pipeline offline:
#     git add -f data/landing_zone/ctgov_raw.json

# OS
.DS_Store
